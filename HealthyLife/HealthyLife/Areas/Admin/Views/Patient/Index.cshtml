@model IEnumerable<HealthyLife.Areas.Admin.Models.PatientViewModel>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
@if (Model != null)
{
    <table class="responsive-table highlight centered">
        <thead>
            <tr>
                <th>@Html.ResourceString(Constants.Patient.SocialSecurityNumber)</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.SocialSecurityNumber</td>
                <td>@Html.RouteLink(Constants.Main.Edit, "Admin_PatientEdit", new {id = item.Id}, new { @class="btn blue" })</td>
                <td>@Html.RouteLink(Constants.Main.Delete, "Admin_PatientDelete", new {id = item.Id}, new { @class="btn red" })</td>
            </tr>
        }
        </tbody>
    </table> 
}